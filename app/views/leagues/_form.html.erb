<%= form_for(@league) do |f| %>
  <% if @league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league.errors.count, "error") %> prohibited this league from being saved:</h2>

      <ul>
      <% @league.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.hidden_field :organization_id, :value=>session[:organization_id] %>
    League Name: <span style="font-style: italic;">(you will define a season next)</span><br/>
    <%= f.text_field :name %><p/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
