<%= form_for(@scoring_method) do |f| %>
  <% if @scoring_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scoring_method.errors.count, "error") %> prohibited this scoring_method from being saved:</h2>

      <ul>
      <% @scoring_method.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
